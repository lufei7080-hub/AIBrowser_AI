# Cursor AI 宪法 (.cursorrules) 

## 🤖 核心开发哲学 (Core Philosophy)
1. **全局思维优先**：拒绝“头痛医头，脚痛医脚”。修复任何一个 Bug 前，必须先分析其底层根源，并确保修复方案能覆盖整个项目中所有相似的边界情况。
2. **极度泛用性**：我们的软件面向无数个不同的网站，系统必须具备极强的动态适应能力，绝不能沦为特定网站的定制脚本。
3. **零容忍“屎山”代码**：坚决贯彻 DRY（Don't Repeat Yourself）和 SOLID 原则。拒绝任何形式的代码冗余、复杂的嵌套逻辑和无意义的硬编码。

---

## 🌍 1. 多站点泛用性与反硬编码 (Multi-Site Universality)
- **绝对禁止硬编码 (No Hardcoding)**：严禁在核心业务逻辑中写死任何特定网站的 URL、DOM 选择器、特定的中文字符或特定的错误文本。
- **抽象与配置驱动 (Config-Driven & Abstraction)**：
  - 必须提取不同网站操作的共性特征，建立标准化的抽象接口。
  - 不同网站的差异化逻辑必须通过“策略模式 (Strategy Pattern)”、“适配器模式 (Adapter Pattern)”或外部配置文件（JSON/YAML）来动态加载。
  - 增加对新网站的支持时，必须且只能通过新增配置或新增适配器来实现，**严禁修改核心引擎代码**。

---

## 🐛 2. 错误修复与多态优化 (Global Error Resolution)
- **由点及面优化 (Scale the Fix)**：当被要求修复一个错误时，不能仅仅针对当前的报错信息加一个 `if/else` 补丁。必须评估该类错误在所有其他网站、其他模块交互中发生的可能性，并提供通用、系统级的拦截和处理方案。
- **宽容的异常捕获**：禁止通过硬匹配特定的 Error Message 字符串来处理异常。应使用标准的错误状态码或通用的错误类进行捕获。
- **边界防御性编程**：在处理不同网站的输入输出、网络响应和 DOM 结构时，必须默认其是“不可靠且随时变化的”，严格进行类型校验、空值安全校验（Optional Chaining）和兜底降级处理。

---

## 🔧 3. 工具与模块化规范 (Tool-Specific Optimization)
- **严格隔离边界**：如果是针对某个具体的独立工具（例如独立爬虫模块、Node.js Sidecar 等）进行修改和优化，其改动必须严格限制在该工具的边界内。
- **禁止副作用外溢**：工具的优化绝不能污染全局命名空间或项目的核心上下文。
- **输入输出标准化**：任何工具模块必须具备明确、标准的接口契约（Type/Interface）。工具内部可以针对其特定的业务场景做极致优化，但对外提供的 API 必须保持统一和稳定。

---

## 🚫 4. 代码洁癖与反“屎山”要求 (Clean Code Enforcement)
- **无情重构重复代码**：一旦发现代码中有两处以上的相似逻辑，必须立即将其抽离为公共函数、Hooks 或独立的复用类。
- **控制复杂度**：
  - 保持函数短小精悍。拒绝超过 50 行的超长函数，超过必须拆分。
  - 拒绝深层嵌套（如回调地狱），妥善使用早期返回 (Early Return) 和现代异步语法 (async/await)。
- **命名即注释**：使用极度准确、具象的英文变量和函数名。代码本身必须是自解释的，删除所有用于解释“糟糕代码逻辑”的废话注释。

---

## 🛠️ 5. 项目架构上下文 (Project Context)
- **技术栈感知**：本项目基于 Tauri 架构，包含系统级脚本管理（如 CloakForge batch 脚本）、Node.js Sidecar 服务以及 Vite 构建系统。
- **跨平台与进程通信**：在处理跨语言/跨进程通信（如 Rust 核心与 Node.js Sidecar 的交互）时，必须考虑端口占用、资源竞争、异步阻塞等全局稳定性问题，确保启动与销毁逻辑的绝对健壮性。